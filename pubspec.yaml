name: WearTuner
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.0.0
  activity_ring: ^0.3.0
  pitch_detector_dart: ^0.0.2
  flutter_audio_capture: ^1.1.4
  pitchupdart: ^0.0.2
  intl: ^0.17.0-nullsafety.2
  time_text: ^0.1.0-nullsafety.0
  google_fonts: ^4.0.3
  wear: ^1.1.0
  wakelock: ^0.6.2
  keep_screen_on: ^2.1.0
  vector_math: ^2.1.0
  animated_text_kit: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
